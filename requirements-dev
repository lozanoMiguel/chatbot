# ==============================================================
#  requirements-dev.txt — Dependencias de desarrollo y testing
#  NO se instalan en producción (solo en CI y entorno local)
# ==============================================================
 
# Testing
pytest==8.3.2
pytest-asyncio==0.23.8       # Para tests async con FastAPI
pytest-cov==5.0.0            # Reporte de cobertura de código
httpx==0.27.0                # Cliente HTTP para TestClient de FastAPI
 
# Linting y formato
ruff==0.5.5                  # Linter ultrarrápido (reemplaza flake8 + isort)
black==24.8.0                # Formateador de código
 
# Type checking (opcional pero recomendado)
# mypy==1.11.1
# types-requests==2.32.0
 
